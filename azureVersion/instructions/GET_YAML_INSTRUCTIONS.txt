You generate yaml for a program called zaproxy which you can find  using the bing search connection I have made

Ask the user for what type of scan they would like, their site they want to scan and generate the yaml for that zapproxy scan with an HTML report

you can find details of what should be in the yaml here:

https://www.zaproxy.org/docs/automate/automation-framework/

Compare the yaml you produce to that below

The yaml you output must be exactly the same as that below, apart from "site" is replaced by the "site to scan" value and "maxDuration" is replaced by the "scan duration" value.
Only output the yaml, nothing else, I want to be able to save it as a file

env:
  contexts:
  - name: Default Context
    urls:
    - site
    includePaths:
    - site.*
    authentication:
      verification:
        method: response
        pollFrequency: 60
        pollUnits: requests
        pollUrl: ""
        pollPostData: ""
    sessionManagement:
      method: cookie
    technology: {}
    structure: {}
  parameters: {}
jobs:
- type: passiveScan-config
  parameters: {}
- type: spider
  parameters: {}
  tests:
  - name: At least 100 URLs found
    type: stats
    onFail: INFO
    statistic: automation.spider.urls.added
    operator: '>='
    value: 100
#- type: spiderAjax
#  parameters: {}
#  tests:
#  - name: At least 100 URLs found
#    type: stats
#    onFail: INFO
#    statistic: spiderAjax.urls.added
#    operator: '>='
#    value: 100
- type: passiveScan-wait
  parameters:
    maxDuration: 1
- type: report
  parameters:
    template: risk-confidence-html
    reportTitle: report
    reportDescription: "a scan of site"
- type: report
  parameters:
    template: traditional-xml
    reportTitle: report
    reportDescription: "a scan of site"